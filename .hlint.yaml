- arguments:
    [
      -XGHC2024,
      -XDuplicateRecordFields,
      -XNoFieldSelectors,
      -XOverloadedRecordDot,
      -XStrictData,
    ]

- group: { name: future, enabled: true }
- group: { name: dollar, enabled: true }

- extensions:
    - default: false
    - name:
        - AllowAmbiguousTypes
        - BinaryLiterals
        - CPP
        - DeriveAnyClass
        - NoStarIsType
        - OverloadedStrings
        - PatternSynonyms
        - RequiredTypeArguments
        - TemplateHaskell
        - TypeData
        - ViewPatterns

- ignore: { name: "Use lambda-case" }

# Version.version appends HBT_COMMIT_SUFFIX, which project.h defaults to the
# empty string for a build with no revision to stamp.
- ignore: { name: "Evaluate", within: Version }
